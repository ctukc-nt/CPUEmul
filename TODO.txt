Схема работы

из файла в промежуточное представление (список строк)
список строк передаётся парсеру строк, который возвращает список классов, содержащих предварительные команды: метка, имя, операнды.
компилятор переводит предварительные команды в директивы процессора, размещаемых в памяти.
